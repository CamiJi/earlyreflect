/**
 * Configuration de domaine — LE SEUL ENDROIT à modifier pour la bascule finale.
 *
 * ÉTAT ACTUEL (maquette sur GitHub Pages) :
 * → les deux lignes actives ci-dessous.
 *
 * BASCULE FINALE (domaine de Mathieu) : remplacer par
 *   export const SITE_URL = 'https://earlyreflect.com';
 *   export const BASE_PATH = '/';
 */
export const SITE_URL = 'https://camiji.github.io';
export const BASE_PATH = '/earlyreflect';
